require 'spec_helper'

describe FileWatcher do
  it 'has a version number' do
    expect(FileWatcher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
